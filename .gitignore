# Build products
.build/
build/

# Release archives (attached to GitHub Releases, not committed)
*.zip

# macOS
.DS_Store

# Xcode (not used by this project, but harmless to guard against)
xcuserdata/
*.xcuserstate
